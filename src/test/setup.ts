// ── Vitest global test setup ───────────────────────────────────────────────────
// Runs before every test file. Imports jest-dom matchers
// (toBeInTheDocument, toHaveTextContent, etc.) into Vitest's expect.

import '@testing-library/jest-dom'
